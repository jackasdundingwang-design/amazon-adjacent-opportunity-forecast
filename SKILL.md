---
name: amazon-adjacent-opportunity-forecast
description: Use when 亚马逊卖家需要从现有ASIN或关键词研究约18个月后的相邻新品、图案配色或套装机会，比较TimesFM与简单模型，冻结预测并用新增月份验证；不用于利润预测。
---

# 相邻新品机会预测

预测对象是“人群 × 问题 × 场景 × 新产品方向 × 站点”，不是原 ASIN 的销量。默认前瞻18个完整自然月。用户已提供 ASIN/站点时直接调查竞品，不重复索取；本项目试点为 B0DCVNDWDR、US，不把此值当其他用户默认值。

## 执行流程

1. 发现当前可调用的卖家精灵工具，读取种子详情、竞品、流量关键词、月度销量和评论；依字段实际响应展开同需求和替代品。只有关键词时按关键词发现竞品，不强求ASIN。保存采集日期、原始来源、查询范围。当前用户将优麦云留至正式发布阶段，研究阶段不查询；其他项目依用户范围决定。达人精灵用于商业内容、场景与商品证据，不能代替人口或Amazon销量。禁止使用 demo 数据冒充真实数据。
2. 形成同人群新问题、同场景互补、问题升级、已证实能力新场景、政策替代五类候选。每条候选有种子关系和证据；不从 Listing 推断制造能力。候选生成不等于优先推荐：在排序、产品方案、出图及预售用途交付前必须读取并执行 `references/opportunity-gates.md`。先主动查高度匹配现有款和反例，再验证增量机制；已知反证必须约束后续动作，不能只写进风险段落。
3. 为各候选定义需求边界及匹配 ASIN 集合。父体总销量与子体销量二选一；统一套装/件单位，关键词市场不能相加。覆盖率未知只报样本量，不冒充全市场。原 ASIN 预测只能用于背景，不替代相邻需求序列。
4. 读取 `references/timesfm.md`，按其环境检查和命令运行数量模块。模型比较或冻结任务同时读取 `references/model-validation.md`；图案、配色和套装机会任务读取 `references/design-opportunities.md`。仅对有真实历史的同需求市场/样本或类目代理预测；新品无历史不补造序列。代理与新品份额之间另列假设，不直接复制代理预测。
5. 检索 X/Reddit 的相关长期社区与热点，去重同源新闻，标注区域和采样偏差。政策核验官方原文、适用范围、发布及生效日期；提案仅作条件情景。市场/社会/政策分别标支持、中性、反向、未核实；无政策红利不编造。
6. MiroFish 是每次运行的强制场景验证门。完成候选生成、匹配款/反例检索和社会政策证据整理后，必须读取并执行 `references/mirofish-run.md`，实际完成模拟并核验数据库或原始日志，才能进入最终排序、产品定型、出图或预售建议。不得用人工叙事假装已运行；服务不可用、运行失败或无法核实结果时，整次技能运行标记为“未完成/阻断”，只交付诊断和补救动作。候选缺少与自身相关的有效自主行动及反向反馈时，候选保持“证据不足”，不得升级。模拟轮次不等于月份；图谱事实与模拟发言隔离，Agent 选择比例不等于购买率，不直接乘到 TimesFM 数值上。
7. 人群独立估算：需求人口=代表性基数×需求发生率；年度 Amazon 买家=需求人口×年度购买概率×条件渠道概率；件数=买家×年订单频次×每单件数。参数须同总体且有来源；缺频次/基数则返回不可估计。搜索、帖子、订单次数均不等于人数。分清人/户、潜在使用者/去重购买者。
8. 仅在 MiroFish 强制门通过后交付机会卡：与种子的关系、产品方向、三类变化机制、增量来源、最接近现有款及反例、MiroFish 项目/模拟标识与有效行动摘要、证据状态、6/12/18个月数量、月份7–18年度件数、月份1–18累计件数、人群口径、条件情景、反证、失效条件和下一验证动作。无法估计的数量明确留空并说明。依机会闸门区分待研究、待验证、优先验证与不建议开发；不为凑齐推荐强行排序。不计算利润。不把排序称为已校准爆品概率。

## 强制质量门槛

- 每个拟推进方案附“负面校验卡”：最接近的反例、失败现象与原因证据、候选为何不会重复失败、验证证据及阻断状态。关键项未知则不得升级为开发或预售建议；图片美化、部件增加和概念免责声明不能解除阻断。执行细则见 `references/opportunity-gates.md` 第6节。
- 每次运行必须保存 MiroFish 的项目 ID、图谱 ID、模拟 ID、实际轮次、原始行动数、有效自主行动数、异常输出、候选相关反向反馈和淘汰理由。种子帖子不算模拟结论；接口计数与数据库不一致时以核验后的原始记录为准，并披露异常。
- 冻结 as_of、完整月截止、数据版本和候选集合。历史回测使用当时可见证据；今天修订的历史只作回顾性测试，不宣称无泄漏。模型预训练污染亦须披露。
- TimesFM 与季节性朴素比较；生产选择须另做多窗口18个月回测和 ETS/阻尼趋势比较，单次留出不能证明稳定优势。
- 模型 q10–q90 是未经本市场校准的名义80%分位范围，不是90%区间，也不是保守/扩张情景。月度分位数不得相加作为年度分位数。
- 14个月数据不足以验证18个月精度。接口可输出不等于预测可信；历史短、结构突变、市场边界变更须降级。
- 商业默认限定 TimesFM 2.5 权重；禁止自动升级至当前非商业的3.0权重。社交数据及 MiroFish 的商业使用另核许可。
- 当前技能提供代理数量脚本和 MiroFish 强制人工编排流程，不声称已实现 MCP 自动适配、XReg、人口数据库、MiroFish 自动调度或完整端到端系统。
